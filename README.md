# odin-recipes

This WebSite will consist of a main index page which will have links to a few basic recipes.

1. I started by creating a new REPOSITORY in the Github and moving it into my machine using the command "git clone (SSH code)".

2. Filled it out with the usual boilerplate HTML and added an 'h1' heading "Odine Recipes", then commiting it using first "git add (file name)" then "git commit -m "(message)"".

3. Created a new directory within the "odin-recipes" and named it "recipes", created a new HTML file within it, naming it with a recipe name.Filled lasagna.html with the usual boilerplate HTML and adding a heading. Commited recipes/ + lasagna.html.

6. Linked lasagna.html to index.html using "a href="Directory/lasagna.html"and commited the change.

7. Filled lasagna.html with the necessary info.
First, added some paragraphs to the body with "p" entitled by an "h2" heading.
Second, added the ingredients using an unordered list with "ul" entitled by an "h2" heading.
Lastly, added the steps to follow using an ordered list with "ol" entitled by an "h2" heading.

8. Create new recipes, filled them with the same body as lasagna.hml and changed their info.

9. Linked rest of recipes to index.html.

10. Commited recipes/ and index.html.